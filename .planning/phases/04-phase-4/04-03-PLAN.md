---
phase: 4
plan: 3
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 4, Plan 3: Plan 3: Scan UI — Button, Panel, Table, Wiring (Local Scan context — depends on Plans 1 & 2)

<task type="auto">
  <n>Comparison table component with sorting</n>
  <context>Local Scan</context>
  <domain>ComparisonTable, SearchResultEntry (from Data Extraction), ScanResult (aggregate)</domain>
  <files>src/content/local-scan/comparison-table.ts, src/content/local-scan/__tests__/comparison-table.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createComparisonTable(entries, onSort) returns HTMLElement
    - Table has 6 column headers: Name, Category, Secondary, Rating, Reviews, Attributes
    - Table renders one row per entry
    - Default sort indicator shown on Rating column (descending)
    - Clicking a header calls onSort(columnKey, direction)
    - Clicking same header toggles direction (asc/desc)
    - Clicking different header sorts by that column (desc first)
    - Null values shown as '—' in cells
    - Secondary categories shown as comma-joined string
    - Empty entries array shows 'No results found' message
  </test>
  <action>
    GREEN phase: Minimal implementation
    - createComparisonTable(entries: SearchResultEntry[], onSort: (key: string, dir: 'asc'|'desc') => void): HTMLElement
    - Build HTML table with thead and tbody
    - Column definitions array for header mapping
    - Sort indicator arrows (▲/▼) in active header
    - Inline styles for compact look: small font, borders, alternating row colors
    - data attributes on headers for sort key
  </action>
  <refactor>
    - Highlight sorted column cells
    - Add row hover effect
  </refactor>
  <verify>pnpm test src/content/local-scan/__tests__/comparison-table.test.ts — all pass</verify>
  <done>
    - Table renders with correct columns and data
    - Sort interaction works
    - Empty/null states handled
  </done>
</task>

<task type="auto">
  <n>Scan button + scan panel + orchestration</n>
  <context>Local Scan</context>
  <domain>ScanButton, ScanPanel, ScanResult (aggregate), SearchResultEntry, PageChanged (domain event), SearchResultsDetected (domain event)</domain>
  <files>src/content/local-scan/scan-button.ts, src/content/local-scan/scan-panel.ts, src/content/local-scan/index.ts, src/content/local-scan/__tests__/scan-orchestrator.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createScanButton(onClick) returns HTMLElement with 'Scan' text
    - Button is styled as floating (fixed position, bottom-right)
    - Button has data-gmb-scan-button attribute
    - createScanPanel(entries, callbacks) returns HTMLElement
    - Panel is wide (~90vw), fixed position, high z-index
    - Panel has header with 'Local Scan' title, Download CSV button, Copy button, Close button
    - Panel contains the comparison table
    - Close button calls onClose
    - Download CSV calls onDownloadCsv
    - Copy calls onCopyClipboard
    - initLocalScan() returns cleanup function
    - When SearchResultsDetected fires, scan button appears
    - Clicking scan button extracts results and opens panel
    - Sorting re-renders table with sorted data
    - Navigation away removes button + panel
    - Cleanup function removes all injected elements
  </test>
  <action>
    GREEN phase: Minimal implementation
    - scan-button.ts: createScanButton(onClick): HTMLElement — floating fixed bottom-right
    - scan-panel.ts: createScanPanel(entries, callbacks): HTMLElement — wide overlay with table
    - index.ts: initLocalScan() orchestrator
      - Listen SearchResultsDetected → inject scan button
      - On click: extractSearchResults → sort by rating desc → createScanPanel
      - Wire sort: re-sort entries, re-render table
      - Wire export: downloadCsv, copyToClipboard
      - Wire close: remove panel, keep button
      - Listen PageChanged (non-search): remove all
      - Return cleanup function
  </action>
  <refactor>
    - Add result count in button ('Scan (12)')
    - Visual feedback on copy ('Copied!')
  </refactor>
  <verify>pnpm test src/content/local-scan/__tests__/ — all pass</verify>
  <done>
    - Scan button appears on search results pages
    - Panel opens with comparison table
    - Sorting works on all columns
    - CSV download and clipboard copy work
    - Cleanup on navigation
  </done>
</task>

<task type="auto">
  <n>Wire local scan into content script</n>
  <context>Local Scan</context>
  <domain>ScanResult (aggregate)</domain>
  <files>src/content/index.ts</files>
  <test>
    - Build succeeds (pnpm build)
    - TypeScript clean (tsc --noEmit)
    - All tests pass
  </test>
  <action>
    - Update src/content/index.ts to import { initLocalScan } from './local-scan'
    - Call initLocalScan() after initAuditPanel()
    - Store cleanup function
  </action>
  <refactor>Clean up any unused imports</refactor>
  <verify>pnpm build succeeds; pnpm test passes all tests; tsc --noEmit clean</verify>
  <done>Content script initializes page detection, category badges, audit panel, and local scan</done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:59:58
