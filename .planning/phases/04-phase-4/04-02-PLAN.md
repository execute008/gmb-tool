---
phase: 4
plan: 2
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 4, Plan 2: Plan 2: Export Utilities (Local Scan context — independent)

<task type="auto">
  <n>CSV and clipboard export formatters</n>
  <context>Local Scan</context>
  <domain>SearchResultEntry (from Data Extraction), ScanResult (aggregate)</domain>
  <files>src/content/local-scan/export-utils.ts, src/content/local-scan/__tests__/export-utils.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - formatAsCsv(entries) returns valid CSV string with header row
    - CSV header: Name,Primary Category,Secondary Categories,Rating,Review Count,Attribute Count
    - CSV values properly escaped (commas in values, quotes in names)
    - CSV handles null fields as empty string
    - formatAsClipboard(entries) returns tab-separated string
    - Clipboard format has same header row (tab-separated)
    - Clipboard format has one line per entry
    - Empty entries array returns header-only string
    - downloadCsv(entries, filename) creates download link and triggers click
    - copyToClipboard(entries) calls navigator.clipboard.writeText
  </test>
  <action>
    GREEN phase: Minimal implementation
    - formatAsCsv(entries: SearchResultEntry[]): string
    - formatAsClipboard(entries: SearchResultEntry[]): string
    - downloadCsv(entries: SearchResultEntry[], filename?: string): void
    - copyToClipboard(entries: SearchResultEntry[]): Promise<void>
    - CSV escaping: wrap in quotes if contains comma/quote/newline, double-escape internal quotes
    - Tab-separated: join fields with \t, rows with \n
    - Download: create blob, URL.createObjectURL, click invisible anchor, revoke URL
  </action>
  <refactor>
    - Extract escaping logic
    - Add timestamp to default CSV filename
  </refactor>
  <verify>pnpm test src/content/local-scan/__tests__/export-utils.test.ts — all pass</verify>
  <done>
    - CSV output is valid with proper escaping
    - Clipboard output pastes correctly into spreadsheets
    - Download triggers file save
    - Null fields handled
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:59:31
