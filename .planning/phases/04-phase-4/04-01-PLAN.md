---
phase: 4
plan: 1
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 4, Plan 1: Plan 1: Search Result Extractor (Data Extraction context — independent)

<task type="auto">
  <n>SearchResultEntry type + batch extractor</n>
  <context>Data Extraction</context>
  <domain>SearchResultEntry (value object), ScanResult (aggregate)</domain>
  <files>src/content/extraction/search-extractor.ts, src/content/extraction/__tests__/search-extractor.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - SearchResultEntry has fields: name, primaryCategory, secondaryCategories, rating, reviewCount, attributeCount
    - extractSearchResults(doc) returns SearchResultEntry[]
    - Extracts name from each listing container
    - Extracts rating from aria-label (parses numeric value)
    - Extracts review count from aria-label
    - Extracts primary category text
    - Extracts secondary categories as array
    - Counts attribute elements per listing
    - Returns empty array when no listings found
    - Handles listings with partial data (null for missing fields)
    - Multiple listings in fixture returns correct count
  </test>
  <action>
    GREEN phase: Minimal implementation
    - Define SearchResultEntry interface
    - extractSearchResults(doc: Document): SearchResultEntry[]
    - Find all listing containers (div[jsaction*='mouseover'] a.hfpxzc, or .Nv2PK as container)
    - For each container, use resolveSelector within that container to get fields
    - Parse rating from aria-label text (e.g. '4.5 stars' -> '4.5')
    - Parse review count from text (e.g. '(1,234)' -> '1,234')
    - Count attribute/tag elements within listing
    - Return typed array
  </action>
  <refactor>
    - Extract parsing helpers (parseRating, parseReviewCount)
    - Add listingContainer selector to selectors.ts searchResults group
  </refactor>
  <verify>pnpm test src/content/extraction/__tests__/search-extractor.test.ts — all pass</verify>
  <done>
    - extractSearchResults returns typed array from fixture
    - All fields extracted correctly
    - Empty DOM returns empty array
    - Partial data handled gracefully
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:59:20
