---
phase: 2
plan: 2
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 2, Plan 2: Plan 2: Category Badge UI + Injection (Category Display context, depends on Plan 1)

<task type="auto">
  <n>CategoryPill component with inline styles</n>
  <context>Category Display</context>
  <domain>CategoryPill (value object), CategoryBadge (aggregate)</domain>
  <files>src/content/category-display/category-pill.ts, src/content/category-display/__tests__/category-pill.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createCategoryPill('Restaurant', 'primary') returns an HTMLElement
    - Primary pill has blue background color (#1a73e8 or similar Google blue)
    - createCategoryPill('Takeout', 'secondary') has gray background (#e8eaed)
    - Pill text content matches the category string
    - Pill has cursor:pointer style
    - Pill has appropriate border-radius for pill shape
    - Pill click handler calls provided onClick callback with category string
    - Pill has font-size and padding appropriate for inline display
  </test>
  <action>
    GREEN phase: Minimal implementation
    - createCategoryPill(category: string, type: 'primary' | 'secondary', onClick: (category: string) => void): HTMLElement
    - Create span element with inline styles (NOT Tailwind — injected into Maps DOM)
    - Primary: background #1a73e8, color white, rounded pill
    - Secondary: background #e8eaed, color #3c4043, rounded pill
    - Common: font-size 12px, padding 2px 8px, border-radius 12px, margin-left 4px, cursor pointer, font-family Google Sans/system-ui
    - Attach click event listener calling onClick(category)
  </action>
  <refactor>
    - Extract color constants
    - Add hover effect via mouseenter/mouseleave
  </refactor>
  <verify>pnpm test src/content/category-display/__tests__/category-pill.test.ts — all pass</verify>
  <done>
    - createCategoryPill returns styled span with correct colors per type
    - Click fires callback with category string
  </done>
</task>

<task type="auto">
  <n>Category badge orchestrator — extract, inject, cleanup</n>
  <context>Category Display</context>
  <domain>CategoryBadge (aggregate), CategoryPill (value object), CategoryData (from Data Extraction)</domain>
  <files>src/content/category-display/category-badges.ts, src/content/category-display/__tests__/category-badges.test.ts, src/content/category-display/index.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - initCategoryBadges() returns a cleanup function
    - When BusinessProfileDetected fires, pills are injected into the DOM
    - Primary category appears as primary-styled pill
    - Secondary categories appear as secondary-styled pills
    - All pills are inside a container with a data attribute (data-gmb-categories) for easy cleanup
    - cleanup() removes all injected pill containers from DOM
    - When PageChanged fires (to non-business-profile), pills are automatically removed
    - When no categories found, no pills are injected (no errors)
    - Clicking a pill changes window.location to /maps/search/{category}+near+me/
    - Re-navigation to new business profile replaces old pills with new ones
  </test>
  <action>
    GREEN phase: Minimal implementation
    - category-badges.ts: Export initCategoryBadges()
    - Listen for BusinessProfileDetected CustomEvent on document
    - On event: call extractCategories(document) from Plan 1
    - Find insertion point: after existing category text using selector config
    - Create container div with data-gmb-categories attribute
    - Create pills via createCategoryPill for primary + each secondary
    - onClick handler: navigate to /maps/search/{encodeURIComponent(category)}+near+me/
    - Listen for PageChanged: if not business-profile, remove container
    - Before injecting, remove any existing container (handles re-navigation)
    - index.ts: barrel export of initCategoryBadges and types
  </action>
  <refactor>
    - Extract injection point finding to separate function
    - Ensure cleanup is thorough (no DOM leaks)
  </refactor>
  <verify>pnpm test src/content/category-display/__tests__/ — all pass</verify>
  <done>
    - Pills injected on business profile detection
    - Pills removed on navigation away
    - Click navigates to category search
    - No errors on empty categories
  </done>
</task>

<task type="auto">
  <n>Wire category display into content script</n>
  <context>Category Display</context>
  <domain>CategoryBadge (aggregate), PageChanged (domain event)</domain>
  <files>src/content/index.ts</files>
  <test>
    RED phase: This is a wiring task — verified by integration
    - Content script imports and calls initCategoryBadges()
    - Build succeeds (pnpm build)
    - No TypeScript errors (tsc --noEmit)
  </test>
  <action>
    GREEN phase:
    - Update src/content/index.ts to import { initCategoryBadges } from './category-display'
    - Call initCategoryBadges() after initPageDetection()
    - Store cleanup function
  </action>
  <refactor>
    - Clean up any debug console.logs
  </refactor>
  <verify>pnpm build succeeds; pnpm test passes all tests</verify>
  <done>
    - Content script initializes both page detection and category display
    - Build produces working dist/content.js
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 13:43:06
