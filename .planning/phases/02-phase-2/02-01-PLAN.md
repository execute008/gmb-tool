---
phase: 2
plan: 1
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 2, Plan 1: Plan 1: Category Extraction (Data Extraction context)

<task type="auto">
  <n>Category extractor with CategoryData type</n>
  <context>Data Extraction</context>
  <domain>CategoryData (value object), SelectorRegistry (shared kernel)</domain>
  <files>src/content/extraction/category-extractor.ts, src/content/extraction/__tests__/category-extractor.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST in category-extractor.test.ts
    - extractCategories returns { primary: string, secondary: string[] } when both found
    - extractCategories returns { primary: string, secondary: [] } when only primary found
    - extractCategories returns { primary: null, secondary: [] } when nothing found
    - Primary category text is trimmed
    - Secondary categories exclude the primary category (no duplicates)
    - Secondary category texts are trimmed
    - Test with HTML fixture containing Google Maps-like category elements
    - Test with empty document (no matching selectors)
  </test>
  <action>
    GREEN phase: Minimal implementation
    - Define CategoryData interface: { primary: string | null; secondary: string[] }
    - Export extractCategories(doc: Document): CategoryData
    - Use resolveSelector(selectors, 'businessProfile', 'primaryCategory', doc) for primary
    - Use resolveAllSelectors(selectors, 'businessProfile', 'secondaryCategories', doc) for secondary
    - Extract textContent, trim whitespace
    - Filter secondary to exclude primary category string
    - Return { primary: null, secondary: [] } on miss
  </action>
  <refactor>
    - Add JSDoc comments
    - Ensure CategoryData type is exported for use by Category Display context
  </refactor>
  <verify>pnpm test src/content/extraction/__tests__/category-extractor.test.ts — all pass</verify>
  <done>
    - extractCategories with fixture returns correct primary + secondaries
    - extractCategories with empty doc returns { primary: null, secondary: [] }
    - Duplicates filtered, text trimmed
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 13:42:38
