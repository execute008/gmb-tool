---
phase: 1
plan: 1
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 1, Plan 1: Plan 1: Project Scaffold (Vite + MV3 + TypeScript + Preact)

<task type="auto">
  <n>Build tooling and TypeScript configuration</n>
  <context>Extension Scaffold (Infrastructure)</context>
  <domain>None — pure infrastructure</domain>
  <files>package.json, tsconfig.json, vite.config.ts, tailwind.config.js, postcss.config.js</files>
  <test>
    RED phase: Write failing tests FIRST.
    - Vitest config resolves and runs an empty test file
    - TypeScript compilation succeeds with strict mode
    - Vite build completes without errors
  </test>
  <action>
    GREEN phase: Minimal implementation to make tests pass.
    - package.json with dependencies: vite, @preact/preset-vite, typescript, vitest, happy-dom, tailwindcss, postcss, autoprefixer
    - tsconfig.json with strict mode, Preact JSX transform (jsxImportSource: preact), path aliases
    - vite.config.ts with Preact plugin, multi-entry build (background, content, popup), output to dist/
    - tailwind.config.js scanning src/ for classes
    - postcss.config.js with tailwind + autoprefixer
  </action>
  <refactor>
    - Ensure path aliases are consistent between tsconfig and vite config
    - Add build scripts to package.json (dev, build, test)
  </refactor>
  <verify>pnpm install succeeds, pnpm build produces dist/, pnpm test runs</verify>
  <done>pnpm build exits 0 with dist/ containing built files; pnpm test exits 0</done>
</task>

<task type="auto">
  <n>MV3 manifest and extension entry points</n>
  <context>Extension Scaffold (Infrastructure)</context>
  <domain>None — pure infrastructure</domain>
  <files>src/manifest.json, src/background/service-worker.ts, src/content/index.ts, src/popup/popup.html, src/popup/popup.ts, src/styles/panel.css</files>
  <test>
    RED phase: Write failing tests FIRST.
    - manifest.json has manifest_version: 3
    - manifest.json has content_scripts matching *://www.google.com/maps/*
    - manifest.json has background.service_worker defined
    - manifest.json has correct permissions (activeTab, storage)
    - Service worker file exports or registers without errors
  </test>
  <action>
    GREEN phase: Minimal implementation to make tests pass.
    - src/manifest.json: MV3 manifest with content_scripts for Google Maps, background service worker, popup action
    - src/background/service-worker.ts: Minimal — chrome.runtime.onInstalled listener, console.log lifecycle
    - src/content/index.ts: Minimal — console.log('GMB Audit Tool loaded on Maps')
    - src/popup/popup.html: Minimal HTML with 'GMB Audit Tool' heading
    - src/popup/popup.ts: Minimal — console.log('popup loaded')
    - src/styles/panel.css: Empty placeholder with Tailwind directives
  </action>
  <refactor>
    - Ensure vite build copies manifest.json to dist/
    - Verify content script entry point is correctly bundled
  </refactor>
  <verify>pnpm build produces dist/ with manifest.json, service-worker.js, content script, popup files; Load unpacked in Chrome — no errors</verify>
  <done>dist/manifest.json exists with manifest_version 3; extension loads in Chrome without errors; console shows 'GMB Audit Tool loaded on Maps' on google.com/maps</done>
</task>

<task type="checkpoint:human-verify">
  <n>Verify extension loads in Chrome</n>
  <context>Extension Scaffold (Infrastructure)</context>
  <domain>None</domain>
  <files>dist/</files>
  <test>N/A — manual verification</test>
  <action>
    - Run pnpm build
    - Open chrome://extensions, enable Developer Mode
    - Click 'Load unpacked', select dist/ folder
    - Navigate to google.com/maps
    - Open DevTools console
  </action>
  <refactor>N/A</refactor>
  <verify>Extension icon appears in Chrome toolbar; No errors in chrome://extensions; Console shows 'GMB Audit Tool loaded on Maps' on Google Maps</verify>
  <done>Extension installed and running on Google Maps without errors</done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 10:44:29
