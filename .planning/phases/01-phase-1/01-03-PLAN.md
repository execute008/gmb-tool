---
phase: 1
plan: 3
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 1, Plan 3: Plan 3: SPA Navigation Detection + Page Classification (depends on Plan 1)

<task type="auto">
  <n>Page Detection Types and Classifier</n>
  <context>Page Detection</context>
  <domain>PageType (enum), PageState (aggregate), PageChanged (domain event), PageClassifier (service)</domain>
  <files>src/content/page-detection/types.ts, src/content/page-detection/classifier.ts, src/content/page-detection/__tests__/classifier.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST in classifier.test.ts
    - classifyPage returns 'business-profile' when URL contains /place/
    - classifyPage returns 'business-profile' when DOM contains business profile elements (data-attrid or role=main with business content)
    - classifyPage returns 'search-results' when URL contains /search/
    - classifyPage returns 'search-results' when DOM contains result list container (div[role=feed])
    - classifyPage returns 'other' for URLs like /maps or /dir/ without matching DOM
    - classifyPage prioritizes URL pattern over DOM when both are available
    - PageType enum has exactly three values: 'business-profile', 'search-results', 'other'
    - PageState type includes url (string), pageType (PageType), timestamp (number)
    - PageChanged event type includes previousState (PageState | null), currentState (PageState)
  </test>
  <action>
    GREEN phase: Implement minimal code to pass tests
    - types.ts: Export PageType as const enum, PageState interface, PageChangedEvent interface
    - classifier.ts: Export classifyPage(url: string, document: Document): PageType
    - URL regex patterns: /\/place\// for business-profile, /\/search\// for search-results
    - DOM selectors as fallback: [data-attrid], div[role=feed]
    - Return 'other' as default fallback
  </action>
  <refactor>
    - Extract URL patterns to named constants
    - Extract DOM selectors to named constants
    - Consider extracting URL-based and DOM-based classification into separate pure functions
  </refactor>
  <verify>pnpm test src/content/page-detection/__tests__/classifier.test.ts — all tests pass</verify>
  <done>
    - classifyPage('https://maps.google.com/place/...', doc) === 'business-profile'
    - classifyPage('https://maps.google.com/search/...', doc) === 'search-results'
    - classifyPage('https://maps.google.com/maps', doc) === 'other'
    - All type exports are properly typed and importable
  </done>
</task>

<task type="auto">
  <n>SPA Navigation Detector with Deduplication</n>
  <context>Page Detection</context>
  <domain>NavigationDetector (service), PageState (aggregate), PageChanged (domain event)</domain>
  <files>src/content/page-detection/detector.ts, src/content/page-detection/__tests__/detector.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST in detector.test.ts
    - createNavigationDetector returns object with start(), stop(), onPageChanged() methods
    - onPageChanged callback fires when URL changes via pushState
    - onPageChanged callback fires when URL changes via replaceState
    - onPageChanged callback fires on popstate event
    - onPageChanged callback fires when MutationObserver detects main content container changes
    - Duplicate events NOT fired when same URL + same pageType detected consecutively
    - PageChanged event contains previousState (null on first detection) and currentState
    - stop() removes all listeners and disconnects MutationObserver
    - start() can be called after stop() to reinitialize
    - MutationObserver targets document.body with subtree: true, childList: true
  </test>
  <action>
    GREEN phase: Implement minimal code to pass tests
    - detector.ts: Export createNavigationDetector(classifier, document, window)
    - Monkey-patch history.pushState and history.replaceState to intercept calls
    - Add popstate event listener on window
    - Create MutationObserver on document.body, debounce mutations (100ms)
    - Track lastPageState to prevent duplicate events
    - Compare URL + pageType for deduplication
    - Return { start, stop, onPageChanged } interface
  </action>
  <refactor>
    - Extract history monkey-patching into separate function
    - Extract MutationObserver setup into separate function
    - Use EventEmitter pattern or simple callback array for onPageChanged subscribers
    - Ensure proper cleanup in stop() to prevent memory leaks
  </refactor>
  <verify>pnpm test src/content/page-detection/__tests__/detector.test.ts — all tests pass</verify>
  <done>
    - detector.onPageChanged(cb) registers callback
    - Simulated pushState triggers callback with correct PageChanged event
    - Simulated DOM mutation triggers callback with correct PageChanged event
    - Second identical navigation does NOT trigger callback (deduplication works)
    - detector.stop() prevents further callbacks
  </done>
</task>

<task type="auto">
  <n>Content Script Integration and Domain Event Dispatch</n>
  <context>Page Detection</context>
  <domain>PageState (aggregate), PageChanged (domain event), BusinessProfileDetected (domain event), SearchResultsDetected (domain event)</domain>
  <files>src/content/page-detection/index.ts, src/content/page-detection/events.ts, src/content/index.ts, src/content/page-detection/__tests__/integration.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST in integration.test.ts
    - initPageDetection() starts detector and returns cleanup function
    - PageChanged event with type 'business-profile' emits BusinessProfileDetected CustomEvent on document
    - PageChanged event with type 'search-results' emits SearchResultsDetected CustomEvent on document
    - PageChanged event with type 'other' does NOT emit specialized event
    - Events are dispatched as CustomEvents with PageState in detail
    - Cleanup function stops detector and removes event listeners
    - Module exports PAGE_DETECTION_EVENTS constant with event names
    - Initial page classification runs on initPageDetection() call
  </test>
  <action>
    GREEN phase: Implement minimal code to pass tests
    - events.ts: Export event name constants and event creator functions
    - index.ts: Export initPageDetection() that wires classifier + detector
    - Dispatch CustomEvents: 'gmb-audit:page-changed', 'gmb-audit:business-profile-detected', 'gmb-audit:search-results-detected'
    - src/content/index.ts: Import and call initPageDetection()
    - Return cleanup function
  </action>
  <refactor>
    - Create barrel export in page-detection/index.ts
    - Ensure event types are properly typed with CustomEvent generics
    - Add JSDoc comments for public API
  </refactor>
  <verify>pnpm test src/content/page-detection/__tests__/integration.test.ts — all tests pass; manual test by loading extension on maps.google.com and checking console for page detection logs</verify>
  <done>
    - initPageDetection() returns cleanup function
    - document.addEventListener('gmb-audit:business-profile-detected', cb) fires when navigating to /place/
    - document.addEventListener('gmb-audit:search-results-detected', cb) fires when navigating to /search/
    - Event detail contains PageState with url, pageType, timestamp
    - No duplicate events on repeated navigation to same page
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 10:45:24
