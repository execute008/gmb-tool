---
phase: 1
plan: 2
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 1, Plan 2: Plan 2: Centralized Selector Config

<task type="auto">
  <n>Selector config definitions grouped by page type</n>
  <context>Data Extraction</context>
  <domain>SelectorRegistry (aggregate) — maps logical field names to CSS selector chains with fallbacks</domain>
  <files>src/config/selectors.ts, src/config/__tests__/selectors.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - selectors object has businessProfile key with name, category, rating, reviewCount, address, phone, website, hours, attributes, placeId fields
    - selectors object has searchResults key with listingName, listingRating, listingReviewCount, listingCategory fields
    - Each field value is a non-empty array of strings (CSS selector fallbacks)
    - Every selector string is a valid CSS selector (does not throw on document.querySelector)
    - TypeScript types enforce the structure: Record<PageType, Record<string, string[]>>
  </test>
  <action>
    GREEN phase: Minimal implementation to make tests pass.
    - src/config/selectors.ts: Export typed selectors object grouped by businessProfile and searchResults
    - Include realistic Google Maps CSS selectors with fallback chains
    - Export TypeScript types: SelectorMap, PageSelectors
  </action>
  <refactor>
    - Add JSDoc comments explaining each selector group
    - Ensure selector names use ubiquitous language (e.g., primaryCategory not cat1)
  </refactor>
  <verify>pnpm test src/config/__tests__/selectors.test.ts — all tests pass</verify>
  <done>selectors.businessProfile.name is ['h1.fontHeadlineLarge', 'h1']; all fields are non-empty string arrays; types export correctly</done>
</task>

<task type="auto">
  <n>Selector resolver with fallback chain</n>
  <context>Data Extraction</context>
  <domain>SelectorRegistry (aggregate) — resolveSelector service</domain>
  <files>src/utils/selector-resolver.ts, src/utils/__tests__/selector-resolver.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST.
    - resolveSelector(selectors, 'businessProfile', 'name', document) returns Element when first selector matches
    - resolveSelector tries second selector when first doesn't match
    - resolveSelector returns null when no selectors match (not an error, no throw)
    - resolveAllSelectors(selectors, 'businessProfile', 'name', document) returns all matching elements from first matching selector
    - resolveSelector works with happy-dom document containing test HTML
    - Test with HTML fixture: div with class fontHeadlineLarge containing 'Test Business'
    - Test fallback: only h1 present (no fontHeadlineLarge class) — still resolves via second selector
  </test>
  <action>
    GREEN phase: Minimal implementation to make tests pass.
    - src/utils/selector-resolver.ts: Export resolveSelector and resolveAllSelectors
    - resolveSelector iterates fallback array, returns first querySelector hit or null
    - resolveAllSelectors iterates fallback array, returns first querySelectorAll hit or empty array
    - Typed with generics for Element subtype
  </action>
  <refactor>
    - Add optional logging parameter for debugging selector misses
    - Extract types to shared types file if needed
  </refactor>
  <verify>pnpm test src/utils/__tests__/selector-resolver.test.ts — all tests pass</verify>
  <done>resolveSelector returns correct element via fallback chain; returns null on total miss; resolveAllSelectors returns NodeList from first matching selector</done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 10:44:49
