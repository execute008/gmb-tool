---
phase: 3
plan: 1
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 3, Plan 1: Plan 1: Business Profile Extractor (Data Extraction context — independent)

<task type="auto">
  <n>BusinessProfileData type and comprehensive extractor</n>
  <context>Data Extraction</context>
  <domain>BusinessProfile (aggregate), SelectorRegistry (shared kernel), BusinessProfileData (value object)</domain>
  <files>src/content/extraction/profile-extractor.ts, src/content/extraction/__tests__/profile-extractor.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - Define BusinessProfileData interface with 20+ fields grouped: name, placeId, cid, primaryCategory, secondaryCategories, rating, reviewCount, address, phone, website, hours, attributes, reviewUrl, directionsUrl, mapsUrl
    - extractBusinessProfile(doc) returns BusinessProfileData
    - Extracts name from h1 selector
    - Extracts rating as string from rating selector
    - Extracts reviewCount as string from reviewCount selector
    - Extracts address, phone, website from contact selectors
    - Extracts placeId from URL or DOM
    - Extracts CID from URL (numeric ID in ludocid param or /maps/place/ URL)
    - Returns null for fields not found (no throw)
    - Generates reviewUrl: https://search.google.com/local/writereview?placeid={placeId}
    - Generates directionsUrl: https://www.google.com/maps/dir//{name}
    - Generates mapsUrl from current page URL
    - Test with comprehensive HTML fixture mimicking Maps DOM
    - Test with minimal/empty DOM — returns object with null fields
  </test>
  <action>
    GREEN phase: Minimal implementation
    - Export BusinessProfileData interface with all fields as string | null (arrays for categories/attributes)
    - Export extractBusinessProfile(doc: Document, url: string): BusinessProfileData
    - Use resolveSelector for each field via selectors.businessProfile
    - Extract textContent, trim, return null on miss
    - Parse Place ID from URL (/place/ path segment) or DOM
    - Parse CID from URL (ludocid parameter)
    - Generate derived URLs (review, directions, maps)
    - Reuse extractCategories from category-extractor for categories
  </action>
  <refactor>
    - Extract URL parsing helpers (getPlaceIdFromUrl, getCidFromUrl)
    - Group extraction by section for readability
    - Add JSDoc
  </refactor>
  <verify>pnpm test src/content/extraction/__tests__/profile-extractor.test.ts — all pass</verify>
  <done>
    - extractBusinessProfile returns 20+ fields from fixture
    - Null fields on empty DOM
    - Generated URLs are correct
    - CategoryData reused from existing extractor
  </done>
</task>

<task type="auto">
  <n>Audit data formatters — text and JSON export</n>
  <context>Data Extraction</context>
  <domain>BusinessProfileData (value object), AuditReport (aggregate)</domain>
  <files>src/content/extraction/audit-formatter.ts, src/content/extraction/__tests__/audit-formatter.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - formatAsText(data: BusinessProfileData) returns multi-line string with labels
    - Text format includes: 'Name: {name}', 'Rating: {rating}', 'Reviews: {reviewCount}', etc.
    - Null fields are omitted from text output (not shown as 'null')
    - formatAsJson(data: BusinessProfileData) returns valid JSON string
    - JSON is pretty-printed (2-space indent)
    - formatSectionAsText(data, section) returns text for a specific section only
    - Sections: 'basicInfo', 'contact', 'reviews', 'attributes', 'links'
  </test>
  <action>
    GREEN phase: Minimal implementation
    - Export formatAsText, formatAsJson, formatSectionAsText
    - Define SECTION_FIELDS mapping: which fields belong to which section
    - formatAsText: iterate fields, skip nulls, format as 'Label: value' lines
    - formatAsJson: JSON.stringify(data, null, 2)
    - formatSectionAsText: filter to section fields only
  </action>
  <refactor>
    - Extract field labels to a constant map
    - Ensure consistent field ordering
  </refactor>
  <verify>pnpm test src/content/extraction/__tests__/audit-formatter.test.ts — all pass</verify>
  <done>
    - Text output is clean, no null values shown
    - JSON is valid and pretty-printed
    - Section formatting works for each section
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:11:07
