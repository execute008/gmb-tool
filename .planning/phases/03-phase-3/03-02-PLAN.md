---
phase: 3
plan: 2
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 3, Plan 2: Plan 2: Audit Panel UI Components (Audit Panel context — depends on Plan 1)

<task type="auto">
  <n>AuditSection collapsible component</n>
  <context>Audit Panel</context>
  <domain>AuditSection (value object), AuditPanel (aggregate)</domain>
  <files>src/content/audit-panel/audit-section.ts, src/content/audit-panel/__tests__/audit-section.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createAuditSection(title, fields, onCopySection) returns HTMLElement
    - Section has a clickable header with title text
    - Section has a content area with field rows (label: value)
    - Clicking header toggles content visibility (collapse/expand)
    - Section starts expanded by default
    - Copy button in header calls onCopySection callback
    - Null/empty fields are not rendered
    - Each field row shows label and value
  </test>
  <action>
    GREEN phase: Minimal implementation
    - createAuditSection(title: string, fields: {label: string, value: string | null}[], onCopySection: () => void): HTMLElement
    - Create container div with header (title + copy button + collapse toggle) and content div
    - Header click toggles content display:none
    - Copy button click calls onCopySection, stopPropagation
    - Filter out null/empty value fields
    - Inline styles (injected into Maps DOM)
  </action>
  <refactor>
    - Add collapse/expand icon indicator
    - Smooth transition for collapse
  </refactor>
  <verify>pnpm test src/content/audit-panel/__tests__/audit-section.test.ts — all pass</verify>
  <done>
    - Section renders with title and fields
    - Collapse toggle works
    - Copy button fires callback
    - Null fields hidden
  </done>
</task>

<task type="auto">
  <n>AuditPanel slide-in container</n>
  <context>Audit Panel</context>
  <domain>AuditPanel (aggregate), AuditSection (value object), BusinessProfileData (from Data Extraction)</domain>
  <files>src/content/audit-panel/audit-panel.ts, src/content/audit-panel/__tests__/audit-panel.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createAuditPanel(data, onClose) returns HTMLElement
    - Panel has fixed position, right: 0, width 380px, full height, high z-index
    - Panel has a header with 'GBP Audit' title and close (X) button
    - Panel has 'Copy as Text' and 'Copy as JSON' buttons in header
    - Panel contains 5 sections: Basic Info, Contact, Reviews, Attributes, Links
    - Close button click calls onClose callback
    - Panel has white background with shadow for visual separation
    - Panel is scrollable (overflow-y: auto on content area)
    - Data fields are distributed to correct sections
  </test>
  <action>
    GREEN phase: Minimal implementation
    - createAuditPanel(data: BusinessProfileData, callbacks: { onClose: () => void, onCopyText: () => void, onCopyJson: () => void }): HTMLElement
    - Fixed position panel: right 0, top 0, width 380px, height 100vh, z-index 10000
    - Header: title, copy buttons, close button
    - Content: 5 AuditSections with appropriate fields from BusinessProfileData
    - Section field mapping: basicInfo=[name, placeId, cid, primaryCategory, secondaryCategories], contact=[address, phone, website], reviews=[rating, reviewCount, reviewUrl], attributes=[attributes], links=[directionsUrl, mapsUrl, website]
    - Inline styles throughout
  </action>
  <refactor>
    - Extract styles to constants
    - Add data-gmb-audit-panel attribute for targeting
  </refactor>
  <verify>pnpm test src/content/audit-panel/__tests__/audit-panel.test.ts — all pass</verify>
  <done>
    - Panel renders with correct dimensions and position
    - All 5 sections present
    - Close and copy buttons functional
    - Scrollable content
  </done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:11:28
