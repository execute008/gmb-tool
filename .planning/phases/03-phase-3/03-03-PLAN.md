---
phase: 3
plan: 3
depends_on: []
must_haves:
  - "[Observable truth this plan delivers]"
---

# Phase 3, Plan 3: Plan 3: Audit Button + Wiring (Audit Panel context — depends on Plans 1 & 2)

<task type="auto">
  <n>Audit button injection and panel orchestration</n>
  <context>Audit Panel</context>
  <domain>AuditButton (value object), AuditPanel (aggregate), BusinessProfileData (from Data Extraction), PageChanged (domain event)</domain>
  <files>src/content/audit-panel/audit-button.ts, src/content/audit-panel/index.ts, src/content/audit-panel/__tests__/audit-orchestrator.test.ts</files>
  <test>
    RED phase: Write failing tests FIRST
    - createAuditButton(onClick) returns HTMLElement styled as a button
    - Button text is 'Audit' with appropriate styling
    - Button click calls onClick callback
    - initAuditPanel() returns cleanup function
    - When BusinessProfileDetected fires, audit button is injected into DOM
    - Clicking audit button opens the panel (panel element appears in DOM)
    - Clicking close on panel removes panel from DOM but keeps button
    - When PageChanged to non-business-profile, button and panel are removed
    - Re-navigation to new business shows new audit button
    - Copy as Text button copies formatted text to clipboard (mock clipboard API)
    - Copy as JSON button copies JSON to clipboard
    - Opening panel on new business re-extracts data (fresh extraction)
  </test>
  <action>
    GREEN phase: Minimal implementation
    - audit-button.ts: createAuditButton(onClick: () => void): HTMLElement
    - index.ts: Export initAuditPanel() that orchestrates everything
    - Listen for BusinessProfileDetected: inject audit button near business name
    - On button click: call extractBusinessProfile, create panel with data, inject into DOM
    - On close: remove panel, keep button
    - On PageChanged (non-business-profile): remove button + panel
    - Copy handlers: use navigator.clipboard.writeText with formatAsText/formatAsJson
    - Section copy: use formatSectionAsText
  </action>
  <refactor>
    - Ensure only one panel can be open at a time
    - Add visual feedback on copy (button text briefly changes to 'Copied!')
  </refactor>
  <verify>pnpm test src/content/audit-panel/__tests__/ — all pass</verify>
  <done>
    - Audit button appears on business profiles
    - Panel opens with extracted data on click
    - Copy buttons work
    - Cleanup on navigation
  </done>
</task>

<task type="auto">
  <n>Wire audit panel into content script</n>
  <context>Audit Panel</context>
  <domain>AuditPanel (aggregate)</domain>
  <files>src/content/index.ts</files>
  <test>
    - Build succeeds (pnpm build)
    - TypeScript clean (tsc --noEmit)
    - All tests pass
  </test>
  <action>
    - Update src/content/index.ts to import { initAuditPanel } from './audit-panel'
    - Call initAuditPanel() after initCategoryBadges()
    - Store cleanup function
  </action>
  <refactor>Clean up any unused imports</refactor>
  <verify>pnpm build succeeds; pnpm test passes all tests; tsc --noEmit clean</verify>
  <done>Content script initializes page detection, category badges, and audit panel</done>
</task>

## Goal
[What this plan achieves from user perspective]

## Context
[Key decisions that affect this plan]

## Tasks

<task type="auto">
  <n>[Task name]</n>
  <files>[affected files]</files>
  <test>[Write tests first — what should pass when done]</test>
  <action>
    [Implementation to make tests pass]
  </action>
  <refactor>[Optional cleanup after green]</refactor>
  <verify>[How to verify]</verify>
  <done>[What "done" looks like]</done>
</task>

---
Created: 2026-03-09 15:11:47
